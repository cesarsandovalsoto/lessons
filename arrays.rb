# arrays
# array are order lists stars white 0
# array = [1, "yey", 3.0]  like python
array = %w[a b c f g] # TODO: if all elements in array are strings this is the metod there are no , in between

# array = Array.new(3) #rybt metodos starts whit capital letters Array
# array = Array.new(3, 'hola')

puts array[2]
print array[0..2] # print don't have /l
